@page
@model StudentCourse.Pages.Students.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Course_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Student_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Student_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Student_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
